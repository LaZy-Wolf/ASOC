[project]
name = "asoc-mcp-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastmcp>=2.0"]

[dependency-groups]
dev = ["pytest>=8.3"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
